#include <stdio.h>
#include <math.h>

int main()
{
   /*Identifying three values to be entered*/
   double n1, n2, n3; 
   
    /*Displays the Instruction on-screen*/
    printf("Enter three numbers: "); 
     /*Scans what the user entered as n1 to n3*/
    scanf("%lf %lf %lf", &n1, &n2, &n3); 

    /*Creating an if Function, which decides whether or not n1 is larger or equal to n2*/
    if (n1>=n2)
    {
        if(n1>=n3)
            printf("%.2lf is the largest number.", n1);
        else
            printf("%.2lf is the largest number.", n3);
    }

    /*Creating an else function, which decides whether or not n2 is greater or equal to n3*/
    else
    {
        if(n2>=n3)
            printf("%.2lf is the largest number.", n2);
        else
            printf("%.2lf is the largest number.",n3);
    }
    return 0;
}
